# EVE Bot 刷怪流程说明

## 整体架构

脚本 `rat_farm_v2.py` 是 EVE Online 自动刷怪机器人，基于 ADB 屏幕采集与控制、OpenCV 模板匹配。

**主循环**：反复执行 步骤1-5 → 退出条件满足 → 重新从步骤3选择新异常

---

## 流程图

```
开始
  ├─ 检测站内/站外
  │    ├─ 站内 → 点击离站 → 等25秒 → 再等20秒点击(1495,1927)随机偏移
  │    └─ 站外 → 跳过
  ├─ 点击侧边界面开关（避免范围重复）
  ├─ 步骤3: 匹配异常空间（排除上次位置，100px阈值）
  │    └─ 点击异常中心 → 等3秒
  ├─ 步骤4: 匹配跃迁按钮（最多等60秒，每3秒检查）
  │    ├─ 找到 → 点击 → 等1秒 → 惯性稳定器 → 等44秒
  │    └─ 找不到 → 清空位置 → 回步骤3选新异常
  └─ 步骤5: 战斗循环（15秒周期）
       ├─ 检测敌人（模板匹配，分组输出）
       ├─ 检测一键锁定 → 点击
       ├─ 装备触发：防御/火控/掠能器
       ├─ 掠能器续期（28秒随机选一个）
       ├─ 装甲维修（60秒开关）
       └─ 退出判断
            ├─ 2轮无目标（无敌人且无锁）→ 跃迁新异常
            ├─ 3轮无锁定 且 3轮敌对数量不变 → 跃迁新异常
            └─ 敌对玩家检测 → 紧急撤退
```

---

## 步骤详解

### 步骤1: 检测是否在站内

- 截图 → 匹配 `undock_btn` 模板
- **站内**：点击离站 → 等25秒 → 再等20秒点击(1495,1927)±15随机偏移 → 继续等5秒
- **站外**：跳过

### 步骤2: 点击侧边界面开关

- 截图匹配 `sidebar_toggle` 模板
- 如果侧边栏按钮在上一轮位置±200px范围内，跳过（避免重复点击）

### 步骤3: 匹配异常空间

- 截图 → 匹配所有异常模板（大/中/小型天使）
- **排除重复**：过滤掉与上次点击位置距离<100px的异常
- **优先级**：大型 > 中型 > 小型（按置信度选择）
- 点击异常中心 → 等待3秒

### 步骤4: 匹配跃迁按钮

- 点击异常后开始等待跃迁按钮（最多60秒，每3秒检查一次）
- 找到跃迁按钮 → 等1秒 → 点击 → 惯性稳定器 → 等44秒
- 60秒找不到 → 清空异常位置记录 → 回步骤3选择其他异常

### 步骤5: 战斗循环（15秒周期）

#### 线程结构
- **主循环线程**：检测敌人、一键锁定、判断退出
- **装备计时线程**：防御模块、火控系统、掠能器续期
- **状态检测线程**：装甲维修、损失检测

#### 敌对检测（15秒周期）
- 模板匹配检测敌对目标
- 分组：护卫舰/巡洋舰/战列舰/战列巡洋舰/工业涅鲁斯/工业座头鲸/工业依米斯/工业巡洋舰/工业战列舰
- 每组只输出最高置信度匹配
- 单个敌人时输出：坐标、置信度、类型

#### 一键锁定（9秒周期）
- 匹配到一键锁定按钮 → 点击
- 冷却时间12秒
- **跃迁后首次锁定**触发装备点击（防御/火控/掠能器）

#### 装备点击流程（收到触发信号后）
1. 等2秒（避免太快点击防御模块）
2. 防御模块：所有缓存位置，每个位置点击一次，标记本周期已点击
3. 综合火控系统：所有缓存位置，每个位置点击一次
4. 旗舰掠能器：所有缓存位置，每个位置点击一次

#### 掠能器续期（28秒周期）
- 随机选择一个掠能器位置点击
- 持续工作直到跃迁

#### 防御模块独立计时（180秒周期）
- 172秒周期结束后（状态=ready），等待180秒才允许重新点击
- 检查 `defense_clicked_this_cycle` 标志，避免本周期重复点击

#### 装甲维修（60秒开关）
- 状态检测线程每30秒检测损失/健康模板
- 检测到损失 → 装甲维修开启 → 60秒后自动关闭

#### 退出条件（15秒周期判断）
1. **2轮无目标**：连续2轮无敌人且无一键锁定 → 跃迁新异常
2. **3轮无锁定且敌对数量不变**：连续3轮检测不到一键锁定 **且** 连续3轮敌对数量相同 → 跃迁新异常
3. **敌对玩家检测**：检测到敌对玩家标签（红色）→ 紧急撤退

#### 退出时处理
- 防御模块激活中 → 点击取消
- 检察官异常模式 → 点击筛选按钮
- 清空异常位置记录 → 主循环回步骤3选择新异常

---

## 参数配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `undock_wait` | 25秒 | 离站等待时间 |
| `anomaly_click_wait` | 3秒 | 点击异常后等待 |
| `warp_wait` | 44秒 | 跃迁后等待 |
| `one_key_lock_interval` | 10秒 | 一键锁定最小间隔 |
| `enemy_check_interval` | 15秒 | 敌对检测周期 |
| `one_key_lock_check_interval` | 9秒 | 一键锁定检测周期 |
| `anomaly_position_threshold` | 100px | 异常位置重复判定阈值 |
| `defense_module_total_cycle` | 172秒 | 防御模块总周期 |
| `defense_module_last_click_time` | 180秒 | 防御模块重新点击间隔 |
| `cap_energy_interval` | 28秒 | 掠能器续期间隔 |
| `armor_repair_wait` | 60秒 | 装甲维修开启后关闭时间 |
| `equip_click_interval` | 2秒 | 装备点击间隔 |

---

## 模板列表

### 界面元素
- `undock_btn` - 离站按钮
- `sidebar_toggle` - 侧边栏开关
- `one_key_lock` - 一键锁定
- `warp_btn` - 跃迁按钮
- `inertia` - 惯性稳定器

### 异常空间
- `anomaly_angel_small` - 天使小型异常
- `anomaly_angel_medium` - 天使中型异常
- `anomaly_angel_large` - 天使大型异常
- `anomaly_angel_inspector` - 天使检察官异常

### 敌对目标（分组匹配，每组只输出最高置信度）
| 分组 | 模板 |
|------|------|
| 敌对护卫舰 | enemy_frigate |
| 敌对巡洋舰 | enemy_cruiser |
| 敌对战列舰 | enemy_battleship |
| 敌对战列巡洋舰 | enemy_battlecruiser |
| 工业涅鲁斯 | industrial_nerus |
| 工业座头鲸 | industrial_zuotouyu |
| 工业依米斯 | industrial_yimisi |
| 工业巡洋舰 | ship_industrial_cruiser |
| 工业战列舰 | ship_industrial_battleship |

### 装备
- `defense_module` - 母舰防御模块
- `fire_control` - 综合火控系统
- `cap_energy` - 旗舰掠能器
- `armor_repair` - 旗舰装甲维修器
- `save_point_00` - 存点

### 状态检测
- `status_loss` - 损失状态
- `status_health` - 健康状态

---

## 危险检测

### 敌对玩家检测
- 区域：`(59, 1875, 397, 1986)`（参考分辨率）
- 检测颜色：RGB(153, 32, 20) 红色
- 检测到敌对玩家 → 立即紧急撤退（存点 → 等3秒 → 取消防御 → 设置终点 → 导航）

### 工业船检测（危险目标）
- `industrial_nerus` - 工业涅鲁斯
- `industrial_zuotouyu` - 工业座头鲸
- `industrial_yimisi` - 工业依米斯
- 检测到工业船 → 标记为危险目标
